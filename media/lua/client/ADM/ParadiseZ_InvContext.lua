--[[ ParadiseZ = ParadiseZ or {}

function ParadiseZ.invContext(player, context, items)    
    if not ParadiseZ.isAdm() then return end

end

Events.OnFillInventoryObjectContextMenu.Add(ParadiseZ.invContext) ]]